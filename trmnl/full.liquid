{%- comment -%}The markup lives in the shared body template. Liquid's render tag is isolated, so every value it needs is passed explicitly.{%- endcomment -%}
{% render "body", layout: "full", stat_limit: 3, row_limit: 12, tick_list: "100,75,50,25,0", screen_height: screen_height, date_labels: date_labels, schemaVersion: schemaVersion, title: title, scope: scope, stats: stats, hasData: hasData, view: view, rows: rows, hours: hours, maximum: maximum, unit: unit, points: points %}
